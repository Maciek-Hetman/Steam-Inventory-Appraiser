version: '3.8'

services:
  # PostgreSQL database
  db:
    image: postgres:18-alpine
    container_name: project_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
  
  # Backend .NET API
  api:
    build:
      context: ./backend
      dockerfile: MyApi/Dockerfile
    container_name: dotnet_api
    restart: on-failure
    ports:
      - "5000:8080" # API will be available at localhost:5000
    depends_on:
      db:
        condition: service_healthy
    environment:
      ConnectionStrings__DefaultConnection: "Host=${DB_HOST};Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD};"
      ASPNETCORE_ENVIRONMENT: Development
    
  # Frontend React
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: project_web
    restart: on-failure
    ports:
      - "3000:3000" # React available at localhost:3000
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - api

volumes:
  postgres_data: 